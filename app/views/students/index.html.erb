<h1>Hello</h1>
<div>
    <% @students.each do |student| %>
    <div>
       <p><%= link_to student.first_name, student_path(student) %></p>
    </div>
    <% end %>
    
    <p>This is where a student's profile information will show up.</p>
    
    <p><%= link_to 'Go to Courses', '/' %></p>
    
</div>
