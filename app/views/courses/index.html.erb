<h1>Hello</h1>
<div>
    <% @courses.each do |course| %>
    <div>
        <p><%= course.course_name %></p>
        <p><%= link_to 'Show Course', course_path(course) %></p>
    </div>
    <% end %>
</div>

