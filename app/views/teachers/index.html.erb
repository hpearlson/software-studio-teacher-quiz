<H1>Hello world</H1>

<% @teachers.each do |teacher| %>
    <tr>
        <td><%= teacher.first_name %></td>
        <td><%= teacher.last_name %></td>
        <td><%= link_to "More information", teacher_path(teacher) %>
    </tr>
<% end %>
                    

<p><%= link_to 'Add Teacher', new_teacher_path %></p>